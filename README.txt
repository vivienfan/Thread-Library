This thread library is designed to be inplement the knowledge on operating systems and threads, an to be used as an alternative to currentlly existing thread library in C.
It contains a number of basic thread functions: initiating threads, creading thread, thread yield, thread exit, thread sleep, and thread wakeup.
These functions as well as other helper functions complete a thread lifecycle.
